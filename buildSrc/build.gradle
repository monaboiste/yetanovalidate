plugins {
    id 'groovy-gradle-plugin'
}

repositories {
    gradlePluginPortal()
}

dependencies {
    // Tagging and artefact versioning
    implementation 'com.netflix.nebula:nebula-release-plugin:19.0.+'
}
