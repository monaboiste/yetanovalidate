plugins {
    id('groovy-gradle-plugin')
}

dependencies {
    implementation(libs.lombok)

    implementation(libs.nebula.release)
    implementation(libs.nebula.lint)

    implementation(libs.sonarlint)
}
